import React from "react";

const Todo = () => {
  // const handleClick = (e) => {
  //   e.preventDefault();
  //   handleToggle(e.currentTarget.id);
  // };

  return (
    <div>
      <h1>haha</h1>
    </div>
  );
};

export default Todo;
